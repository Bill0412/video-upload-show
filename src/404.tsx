import React from 'react';

// route: /* other than / and /show
export default class PageNotFound extends React.Component {
    render() {
      return(
        <h2>Page not Found!</h2>
      )
    }
  }